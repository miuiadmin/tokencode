# 技能（Skills）

技能是可复用的提示与脚本包，放在 `.tokencode/skills/` 下。每个技能包含一份 `SKILL.md`（描述与触发条件）以及可选的脚本、子 agent 定义。

TokenCode 内置了若干维护者工作流技能（如 `tokencode-bug`、`tokencode-issue-digest`、`tokencode-pr-body`）。你也可以在用户级或项目级 `.tokencode/skills/` 下添加自有技能。
